import { createGlobalStyle } from "styled-components";

export const GlobalStyle = createGlobalStyle`
  body, #root {
    padding: 0 0 91.6px 0;
    margin: 0;
    box-sizing: border-box;
    background-color: #202635;
    height: 100%;
    width: 100%;

    font-family: 'Mulish', sans-serif;
  }
`;
